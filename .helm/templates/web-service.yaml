apiVersion: v1
kind: Service
metadata:
  name: {{ regexReplaceAll "\\W+" (lower .Chart.Name) "_"}}-web
spec:
  selector:
    app: {{ regexReplaceAll "\\W+" (lower .Chart.Name) "_"}}-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
